<div class="container px-5 px-sm-0 py-5">
  <div class="row">
    <h2>新規会員登録</h2>
  </div>
  
  <div class="row py-5">
    <div class="col-md-12 offset-md-2">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= render "public/shared/error_messages", resource: resource %>
    <table cellpadding="15">
      <tr>
        <td><%= f.label :名前 %>　　　　　　　（姓）</td>
        <td><%= f.text_field :last_name %></td>
        <td>（名）<%= f.text_field :first_name %></td>
      </tr>
      <tr>
        <td><%= f.label :フリガナ %>　　　　    （セイ）</td>
        <td><%= f.text_field :last_name_kana %></td>
        
        <td>（メイ）<%= f.text_field :first_name_kana %></td>
      </tr>
    </table>
    <table cellpadding="15">
      <tr>
        <td><%= f.label :メールアドレス %></td>
        <td><%= f.email_field :email, autofocus: true, autocomplete: "email" %></td>
      </tr>
      <tr>
        <td><%= f.label :郵便番号（ハイフンなし） %></td>
        <td><%= f.text_field :postal_code %></td>
      </tr>
      <tr>
        <td><%= f.label :住所 %></td>
        <td><%= f.text_field :address, :size=>"58" %></td>
      </tr>
      <tr>
        <td><%= f.label :電話番号 %></td>
        <td><%= f.text_field :telephone_number %></td>
      </tr>
      <tr>
        <td>
          <%= f.label :パスワード %>
          <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> 文字以上)</em>
          <% end %>
        </td>
        <td><%= f.password_field :password, autocomplete: "new-password" %></td>
      </tr>
      <tr>
        <td><%= f.label :パスワード（確認用） %></td>
        <td><%= f.password_field :password_confirmation, autocomplete: "new-password" %></td>
      </tr>
      <tr>
        <td></td>
        <td><input class="btn btn-success" type="submit" value="新規登録"></td>
      </tr>
    </table>
    <% end %>
    </div>
  </div>

<h1>既に登録済みの方</h1>
 <%= link_to "こちら", new_customer_session_path %>から新規登録を行ってください。

</div>