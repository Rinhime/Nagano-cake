<h1>（会員用）ショッピングカート</h1>
<h1>数量変更、カート削除の要素を</h1>

<table>
  <thead>
    <th>商品名</th>
    <th>単価(税込)</th>
    <th>小計</th>
  </thead>
  <tbody>
  <% @cart_items.each do |cart_item| %>
    <tr>
      <td><%= image_tag cart_item.get_image(100,100) %></td>
      <td>
        <%= cart_item.item.name %>
      </td>
      <td><%= cart_item.item.with_tax_price %></td>
      <td><%= cart_item.subtotal %></td>
        <% @total += cart_item.subtotal %>
    </tr>
  <% end %>
  </tbody>
</table>